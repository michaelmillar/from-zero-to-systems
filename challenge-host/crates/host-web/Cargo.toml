[package]
name = "host-web"
version = "0.1.0"
edition = "2021"

[dependencies]
host-protocol = { path = "../host-protocol" }
host-runtime = { path = "../host-runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
